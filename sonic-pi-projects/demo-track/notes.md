# Demo track notes

- Run `main.pi` from VS Code (Sonic Pi extension).
- `live_loops/beat.pi` holds drums; edit independently and rerun selection to update live.
- Add more parts in `live_loops/` and `load` them from `main.pi`.
